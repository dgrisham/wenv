#!/usr/bin/env zsh

wenv_def() {
    WENV_DIR=""
    WENV_DEPS=()
    WENV_EXTENSIONS=('c')

    startup_wenv() {}
    shutdown_wenv() {}
    bootstrap_wenv() {}
}

# for c extension
# declare -Ag wenv_dirs=()

# for edit extension
# declare -Ag wenv_files=()
