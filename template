#!/usr/bin/env zsh

wenv_dir=""
wenv_deps=()
wenv_extensions=('c')

startup_wenv() {}
shutdown_wenv() {}
bootstrap_wenv() {}

((only_load_wenv_vars == 1)) && return 0

# for c extension
# declare -Ag wenv_dirs=()

# for edit extension
# declare -Ag wenv_files=()
