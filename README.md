WENV -- A Simple Working Environment Framework
==============================================

Perpetual WIP, likely to have bugs. Needs documentation.

Requirements:

-   Add to `.zshrc`.
    -   `eval ZSH_INIT`
    -   `ORIGINAL_PS1="$PS1"`
-   Add to `tmux.conf`
